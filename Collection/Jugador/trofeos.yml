info:
  name: trofeos
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/jugadores/1/trofeos?pagina=0&tamano=10
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
