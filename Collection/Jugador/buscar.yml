info:
  name: buscar
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/jugadores/buscar?username=imanol
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false