info:
  name: actualizar
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/jugadores/1
  body:
    type: json
    data: |-
      {
        "usuarioId": 1,
        "equipoFavorito": "Real Madrid"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false