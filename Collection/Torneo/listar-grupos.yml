info:
  name: listar grupos
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8080/torneos/1/grupos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false