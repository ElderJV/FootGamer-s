info:
  name: proxima ronda
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/torneos/1/proximaRonda
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false