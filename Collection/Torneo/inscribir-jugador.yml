info:
  name: inscribir jugador
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/torneos/1/jugadores/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false