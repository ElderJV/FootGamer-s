info:
  name: generar llaves
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8080/torneos/1/llaves
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false