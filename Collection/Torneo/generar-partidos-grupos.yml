info:
  name: generar partidos de grupos
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/torneos/1/partidos/grupos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false