info:
  name: me
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/auth/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false