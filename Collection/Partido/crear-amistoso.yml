info:
  name: crear amistoso
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/partidos
  body:
    type: json
    data: |-
      {
        "torneoId": null,
        "tipoPartido": "AMISTOSO",
        "fecha": "2026-07-05"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false