info:
  name: asignar bando ganador
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8080/partidos/1/ganador/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false