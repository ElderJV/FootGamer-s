info:
  name: listar por grupo
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/partidos/grupo/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false