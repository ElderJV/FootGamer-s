info:
  name: listar por torneo
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/partidos/torneo/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false