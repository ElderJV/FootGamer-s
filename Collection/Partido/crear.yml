info:
  name: crear
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/partidos
  body:
    type: json
    data: |-
      {
        "torneoId": 1,
        "tipoPartido": "TORNEO",
        "fecha": "2026-07-05"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false