info:
  name: listar
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/categorias?pagina=0&tamano=10
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false