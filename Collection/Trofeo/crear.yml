info:
  name: crear
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/trofeos
  body:
    type: json
    data: |-
      {
        "nombre": "Copa de la Liga",
        "fecha": "2026-12-20",
        "jugadorId": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
