spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: FootGamers
  datasource:
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    url: ${DB_URL}
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: ${JWT_SECRET:clave-desarrollo-para-firmar-jwt-footgamers-2026}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}